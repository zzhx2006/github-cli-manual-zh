# gh pr close

关闭拉取请求

```
gh pr close {<number> | <url> | <branch>} [flags]
```

## 选项

`-c`, `--comment <string>`

发表关闭评论（closing comment）

`-d`, `--delete-branch`

关闭后删除本地和远程分支

## 从父命令继承的选项

`-R`, `--repo <[HOST/]OWNER/REPO>`

使用 [HOST/]OWNER/REPO 格式选择另一个仓库

## 另请参见

- [gh pr](/gh_pr)